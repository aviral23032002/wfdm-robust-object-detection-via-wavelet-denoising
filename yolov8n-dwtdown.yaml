nc: 12
scales:
  n: [0.33, 0.25, 1024]

backbone:
  - [-1, 1, Conv, [64, 3, 2]]    # 0-P1/2 (Outputs 16)
  
  # --- THE WAVELET DOWNSAMPLERS (Passing only Output Channels 'c') ---
  - [-1, 1, DWTDown, [32]]       # 1-P2/4 (YOLO tracker sees 32)
  - [-1, 3, C2f, [128, True]]    # 2 (Expects 32, Gets 32)
  
  - [-1, 1, DWTDown, [64]]       # 3-P3/8 (YOLO tracker sees 64)
  - [-1, 6, C2f, [256, True]]    # 4 (Expects 64, Gets 64)
  
  - [-1, 1, DWTDown, [128]]      # 5-P4/16 (YOLO tracker sees 128)
  - [-1, 6, C2f, [512, True]]    # 6 (Expects 128, Gets 128)
  
  - [-1, 1, DWTDown, [256]]      # 7-P5/32 (YOLO tracker sees 256)
  - [-1, 3, C2f, [1024, True]]   # 8 (Expects 256, Gets 256)
  # -------------------------------------------------------------------

  - [-1, 1, SPPF, [1024, 5]]     # 9

head:
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 10
  - [[-1, 6], 1, Concat, [1]]  # 11 
  - [-1, 3, C2f, [512]]        # 12
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 13
  - [[-1, 4], 1, Concat, [1]]  # 14 
  - [-1, 3, C2f, [256]]        # 15
  - [-1, 1, Conv, [256, 3, 2]] # 16
  - [[-1, 12], 1, Concat, [1]] # 17 
  - [-1, 3, C2f, [512]]        # 18
  - [-1, 1, Conv, [512, 3, 2]] # 19
  - [[-1, 9], 1, Concat, [1]]  # 20 
  - [-1, 3, C2f, [1024]]       # 21
  - [[15, 18, 21], 1, Detect, [nc]]  # 22